<h1>Tags Index</h1>


<ul id="index">
	<% @tags.each do |tag| %>
		<li>
			<%= link_to tag.name, tag_path(tag) %>
		</li>	
	<% end %>
</ul>

<p><%= link_to "<< Back to Articles List", articles_path %></p>